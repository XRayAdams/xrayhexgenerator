output: dist/
releases:
  - name: dev
    jobs:
      - name: linux-deb
        package:
          platform: linux
          target: deb
          build_args:
            enable-experiment: records
      - name: linux-rpm
        package:
          platform: linux
          target: rpm
          build_args:
            enable-experiment: records
#      - name: linux-appimage
#        package:
#          platform: linux
#          target: appimage
#          build_args:
#            enable-experiment: records
